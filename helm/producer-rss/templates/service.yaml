apiVersion: v1
kind: Service
metadata:
  name: {{ include "producer-rss.fullname" . }}
  labels:
    {{- include "producer-rss.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "producer-rss.selectorLabels" . | nindent 4 }}
